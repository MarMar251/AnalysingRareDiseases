/** User roles used across the app */
export enum UserRole {
  ADMIN  = "admin",
  DOCTOR = "doctor",
  NURSE  = "nurse",
}

/** Gender values reused by multiple entities */
export enum GenderEnum {
  MALE   = "male",
  FEMALE = "female",
}
